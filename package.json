{
  "name": "collective-intelligence-terminal-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for index.html inline logic",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  }
}
